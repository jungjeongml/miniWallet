import styled from "styled-components";
export const AppWrapper = styled.div`
  max-width: 30rem;
  height: 42rem;
  margin: 0 auto;
  display: flex;
  transform: translateY(10%);
  background: black;
`;
